[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "taskferry-dramatiq"
version = "0.2.0"
description = "Dramatiq task backend for Taskferry — Redis or RabbitMQ backed tasks."
readme = "README.md"
requires-python = ">=3.12"
license = "Apache-2.0"
license-files = ["LICENSE"]
authors = [{ name = "Taskferry authors" }]
keywords = ["taskferry", "dramatiq", "redis", "rabbitmq", "tasks"]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: Apache Software License",
    "Programming Language :: Python :: 3 :: Only",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Typing :: Typed",
]
dependencies = ["taskferry>=0.2,<0.3"]

[project.optional-dependencies]
dramatiq = ["dramatiq>=1.16"]
redis = ["dramatiq[redis]>=1.16"]
rabbitmq = ["dramatiq[rabbitmq]>=1.16"]

[project.entry-points."taskferry.backends"]
dramatiq = "taskferry_dramatiq:make_backend"

[project.urls]
Homepage = "https://github.com/taskferry/taskferry-dramatiq"
Documentation = "https://taskferry.dev"
Source = "https://github.com/taskferry/taskferry-dramatiq"

[tool.hatch.build.targets.wheel]
packages = ["src/taskferry_dramatiq"]

[tool.hatch.build.targets.sdist]
include = ["src", "README.md", "CHANGELOG.md", "LICENSE"]
